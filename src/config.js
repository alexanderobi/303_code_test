const host = 'localhost';
const user = 'root';
const password = '';
const dialect = 'mysql';
const database = 'test_303';

module.exports = {
  database: {
    host,
    user,
    password,
    dialect,
    database,
  },
};
